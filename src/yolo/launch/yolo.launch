<launch>

    <!-- Args -->
    <arg name="cuda"                    default="true"/>
    <arg name="render"                  default="true"/>
    <arg name="model"                   default="robotseg_small_200.pt"/>
    <arg name="realsense"               default="false"/>

    <!-- RS Camera -->
    <include if="$(arg realsense)" file="$(find yolo)/launch/rs_rgbd.launch">
        <arg name="output"              value="log"/>
        <arg name="depth_width"         value="640"/>
        <arg name="depth_height"        value="480"/>
        <arg name="depth_fps"           value="30"/>
        <arg name="color_width"         value="640"/>
        <arg name="color_height"        value="480"/>
        <arg name="color_fps"           value="30"/>
    </include>

    <!-- YOLO -->
    <node pkg="yolo" type="detect.py" name="yolo" output="screen">
        <param name="cuda" value="$(arg cuda)"/>
        <param name="render" value="$(arg render)"/>
        <param name="model" value="$(find yolo)/models/$(arg model)"/>
    </node>

    <!-- Rviz -->
    <!-- <node pkg="rviz" type="rviz" name="rviz" required="true"/> -->

</launch>